% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot-biosynthesis.R
\name{autoplot.glyenzy_biosynthesis_network}
\alias{autoplot.glyenzy_biosynthesis_network}
\title{Plot a glycan biosynthesis network}
\usage{
\method{autoplot}{glyenzy_biosynthesis_network}(
  object,
  show_enzyme = TRUE,
  size = 0.4,
  node_gap = 0.25,
  level_gap = 0.6,
  width = 6,
  height = 6,
  units = c("in", "cm", "mm"),
  show_linkage = FALSE,
  orient = c("left", "right", "up", "down"),
  color_edge = FALSE,
  enzyme_label_style = c("condensed", "full"),
  highlight_target = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{glyenzy_biosynthesis_network} object returned by a
biosynthesis function.}

\item{show_enzyme}{Logical. Whether to label reaction edges with enzyme or
virtual-enzyme names. Labels that would be too small to render after panel
fitting are hidden with a warning.}

\item{size}{Positive numeric whole-cartoon scale multiplier passed to
\code{\link[glydraw:geom_node_glycan]{glydraw::geom_node_glycan()}}. Defaults to \code{0.4}.}

\item{node_gap}{Non-negative physical clearance, in inches, between glycan
nodes at the same rank.}

\item{level_gap}{Non-negative physical clearance, in inches, between
adjacent ranks. Increase this when enzyme labels are unusually long.}

\item{width, height}{Positive, finite figure dimensions. Networks larger than
the available figure are scaled proportionally, while smaller networks
retain their natural size and are centered in the figure.}

\item{units}{Units for \code{width} and \code{height}. One of \code{"in"} (the default),
\code{"cm"}, or \code{"mm"}.}

\item{show_linkage}{Logical. Whether glycan linkage annotations are shown.
Defaults to \code{FALSE} for a compact network.}

\item{orient}{Glycan drawing orientation passed to
\code{\link[glydraw:geom_node_glycan]{glydraw::geom_node_glycan()}}. One of \code{"left"}, \code{"right"}, \code{"up"}, or
\code{"down"}; the default is \code{"left"}.}

\item{color_edge}{Logical. Whether reaction edges and enzyme labels use the
SNFG color of the residue added by each reaction. Defaults to \code{FALSE},
which draws them in dark grey. Concrete and virtual reactions use solid
and dashed lines, respectively, in both modes. Reactions without one
unambiguous added residue remain dark grey.}

\item{enzyme_label_style}{How candidate enzymes on one reaction edge are
labeled. \code{"condensed"} (the default) groups names with the same prefix and
terminal number, for example, \code{"B4GALT1/2/3, B3GALT3/4"}. \code{"full"} keeps
complete names separated by \code{" / "}.}

\item{highlight_target}{Logical or \code{NULL}. When \code{TRUE}, target glycans are
drawn normally and all other glycans are semi-transparent. The default,
\code{NULL}, highlights targets in multi-target networks but not in
single-target networks. Reaction edges and enzyme labels are unchanged.
This argument cannot be \code{TRUE} for networks returned by
\code{\link[=path_biosynthesis]{path_biosynthesis()}} or \code{\link[=path_biosynthesis_virtual]{path_biosynthesis_virtual()}}.}

\item{...}{Additional glycan appearance arguments accepted by
\code{\link[glydraw:glycanGrob]{glydraw::glycanGrob()}}, such as \code{node_size}, \code{colors}, or \code{style}.}
}
\value{
A ggraph/ggplot object with a fixed-size, collision-aware layered
panel centered in the requested figure dimensions.
}
\description{
\code{autoplot.glyenzy_biosynthesis_network()} draws the typed networks returned
by \code{\link[=trace_biosynthesis]{trace_biosynthesis()}}, \code{\link[=trace_biosynthesis_virtual]{trace_biosynthesis_virtual()}},
\code{\link[=path_biosynthesis]{path_biosynthesis()}}, and \code{\link[=path_biosynthesis_virtual]{path_biosynthesis_virtual()}}. A layered DAG
layout determines the node positions while accounting for converging
biosynthesis routes. Candidate enzyme names stored on each reaction edge
are combined into one label.
}
\details{
Glycan dimensions are measured before the network is laid out. Nodes at the
same rank are separated by their rendered bounds, while rectangular edge
caps stop arrows outside the source and target glycans. The plot uses a
fixed-size panel inside a requested figure canvas so these clearances remain
physical rather than changing with the coordinate range.

Concrete enzyme reactions use solid edge shafts, while virtual enzyme
reactions use dashed shafts; arrowheads remain solid in both cases. This
convention is documented rather than repeated in an in-plot legend.
}
\section{Figure size}{

\code{width}, \code{height}, and \code{units} describe the complete base figure returned by
this method. Use the same dimensions with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} or the
corresponding knitr/Quarto figure options. Titles, legends, or margins added
after this method returns may require additional output space.
}

\examples{
\dontrun{
network <- trace_biosynthesis_virtual(
  "GlcNAc(b1-4)Gal(b1-3)GalNAc(a1-"
)
network_plot <- ggplot2::autoplot(network, width = 8, height = 5)
ggplot2::ggsave(
  "biosynthesis-network.png",
  network_plot,
  width = 8,
  height = 5,
  units = "in"
)
}
}
